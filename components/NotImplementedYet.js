import React from 'react';
import {Alert} from 'react-native';

export function NotImplementedYet() {
  return () => Alert.alert("Lo siento!", "Esta funcionalidad todavía no se encuentra implementada :(")
}
